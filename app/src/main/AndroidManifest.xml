<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TerraBreed"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config"
        tools:targetApi="31">

        <!-- ══════════════ ACTIVITIES ══════════════ -->
        <activity
            android:name=".activities.SplashActivity"
            android:theme="@style/Theme.TerraBreed.Splash"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.TerraBreed" />

        <activity
            android:name=".activities.AIVoiceCallActivity"
            android:exported="false"
            android:theme="@style/Theme.TerraBreed"
            android:screenOrientation="portrait" />

        <activity
            android:name=".activities.SettingsActivity"
            android:exported="false"
            android:theme="@style/Theme.TerraBreed" />

        <activity
            android:name=".activities.IncubationDetailActivity"
            android:exported="false"
            android:theme="@style/Theme.TerraBreed" />

        <!-- ══════════════ WIDGET PROVIDERS ══════════════ -->

        <!-- Widget 1: Temperature Chart -->
        <receiver
            android:name=".widgets.TemperatureWidget"
            android:exported="true"
            android:label="@string/widget_temp_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/temperature_widget_info" />
        </receiver>

        <!-- Widget 2: Humidity Chart -->
        <receiver
            android:name=".widgets.HumidityWidget"
            android:exported="true"
            android:label="@string/widget_humid_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/humidity_widget_info" />
        </receiver>

        <!-- Widget 3: Incubation Status -->
        <receiver
            android:name=".widgets.IncubationWidget"
            android:exported="true"
            android:label="@string/widget_incubation_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/incubation_widget_info" />
        </receiver>

        <!-- Widget 4: AI Voice Call -->
        <receiver
            android:name=".widgets.AIVoiceWidget"
            android:exported="true"
            android:label="@string/widget_ai_voice_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="com.terrabreed.app.ACTION_VOICE_CALL" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/ai_voice_widget_info" />
        </receiver>

        <!-- Widget Update Service -->
        <service
            android:name=".widgets.WidgetUpdateService"
            android:exported="false" />

        <!-- Boot Receiver -->
        <receiver
            android:name=".utils.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
